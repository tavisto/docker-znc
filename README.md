#  ZNC bouncer based on alpine

To get started:

    docker run -it -v /path/to/your/configs:/home/znc/.znc tavisto/znc --makeconf

To run it:

    docker run -d -v /path/to/your/configs:/home/znc/.znc tavisto/znc
